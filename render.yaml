services:
  - type: web
    name: healthgraph
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn healthgraph.app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
    autoDeploy: true
